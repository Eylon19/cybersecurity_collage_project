# ============================================
# הגדרות סביבה — העתק קובץ זה ל-.env
# ============================================
# חשוב: הקובץ חייב להיקרא בדיוק ".env" (לא .env.txt)
# ב-VS Code: File → Save As → .env

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password_here
DB_NAME=cyber_awareness
SESSION_SECRET=change-this-to-a-long-random-string
PORT=3000
